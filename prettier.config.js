/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "es5",
  printWidth: 80,
  useTabs: false,
  quoteProps: "as-needed",
  bracketSpacing: true,
  arrowParens: "avoid",
  endOfLine: "lf",
}

module.exports = config